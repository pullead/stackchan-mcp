# StackChan 项目约定

## 表情联动

对话过程中，在**语气有明显变化的节点**调用 `self.robot.set_emotion` 让机器人跟着变脸。
不要每轮回复都动 —— 那样很聒噪。平时保持 `neutral`。

值得变表情的时刻：

| 场景 | 表情 |
|---|---|
| 任务完成、测试通过、问题解决 | `happy` |
| 用户表扬、道谢 | `loving`（会带爱心装饰） |
| 报错、构建失败、发现 bug | `sad` |
| 我自己搞错了、需要更正之前的说法 | `embarrassed`（会带脸红） |
| 遇到反常现象、需要排查、假设被推翻 | `confused` |
| 正在长时间思考或分析 | `thinking` |
| 结果出乎意料（比如实测推翻了预期） | `surprised`，很意外时用 `shocked`（带晕眩） |
| 长时间没有交互、要休息了 | `sleepy`（注意会低头并停掉待机动作） |

变完表情后**不要额外调 `show_text` 重复说明**，屏幕文字留给真正需要显示的内容。

表情默认保持 15 秒（`hold_seconds`），之后自然回到待机小动作，不用手动复位。

## 气泡摘要

每次回答时，把**核心结论压成一句话**（15-30 字）用 `self.robot.show_text` 送到气泡上，
完整回答留在终端。机器人看起来就像在「说重点」。

气泡宽度只够一次显示约 12 个汉字，超出部分固件会自动横向循环滚动
（`LV_LABEL_LONG_MODE_SCROLL_CIRCULAR`，无需额外处理）。

**滚动时间要给够**：约 37 个汉字滚完一轮需要 6-8 秒。`duration_seconds` 按
`max(6, 字数 / 5)` 估算，别让文字滚到一半就被清掉。

不要往气泡里塞代码、表格、文件路径或链接 —— 小屏幕上是乱码，没有可读性。

## 不要碰的东西

**不要主动设置 LED 颜色。** 机器人两侧的灯带是小智语音助手的状态指示，由固件驱动：
LISTENING 绿、STANDBY 熄灭、SPEAKING 蓝。`set_led_color` 是直接写死颜色，固件只在状态
切换时才会覆盖，随手设一个颜色会一直亮着不走。除非用户明确要求改灯色，否则别碰。

**不要在转头指令里顺带塞 `pitch`。** `set_head_angles` 的 `pitch` 默认 `-9999` 表示
「不改变」。用户定的默认俯仰角是 **8**，塞别的值会让机器人显得在仰头。只有用户明确
说抬头/低头时才动 `pitch`。

## MCP 操作不发声

通过 MCP 控制机器人时**只在屏幕上显示文字，不要发出任何声音**。

表情、转头、LED、`show_text`、拍照本来就都是静默的。唯一会发声的是
`create_reminder` —— 提醒到点会播放通知音，只在用户明确要求设提醒时才用。

**不要为此调整设备音量。** 音量归小智语音助手用，它该说话时要能说话。
用户默认音量是 **10**。

## 动作

需要连贯动作时用设备端工具 `self.robot.nod` / `shake_head` / `look_around`，
**不要在 PC 侧循环发 `set_head_angles`** —— 设备开了 WiFi 省电，密集请求延迟会累积
恶化（实测点头 3 下：设备端 2.6 秒，PC 侧连发 9 秒且逐次变慢）。

这些工具立即返回，动作在设备后台跑完，围绕当前姿态摆动并回到原位。
同一时刻只能有一个动作在跑，重复调用会返回 "Another motion is still running"。

## 固件开发

改固件前先读 `DEVLOG.md`，里面记了全部踩过的坑。几条最要紧的：

- 构建走项目根的 `.\build.ps1`，它内置了补丁完整性校验
- 遇到莫名其妙的构建错误**先重试一次**，这台机器有偶发的进程/文件系统故障
- 固件版本号锁在 `9.9.9`，用来关闭小智云的自动 OTA，**不要改低**
- 改动要同步回 `firmware-patch/`，那是仓库里保存固件改动的唯一形式
